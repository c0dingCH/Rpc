syntax = "proto3"; // protobuf version

package test; // namespace in cpp

option cc_generic_services = true;

message LoginRequest{
  bytes name = 1;
  bytes pwd = 2;
}

message LoginResponse{
  bool success = 1;
}

service UserService{
  rpc Login(LoginRequest) returns(LoginResponse);
}


